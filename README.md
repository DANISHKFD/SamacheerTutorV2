# 🎓 GemTutor — AI Tutor for Tamil Nadu State Board Students (Classes 8–10)

A full-stack RAG-powered chatbot that answers questions from Maths, Science, and Social Science using Samacheer Kalvi textbook content for Classes 8, 9, and 10. Built with Flask + FAISS + Sentence Transformers + Google Gemini.

---

## 📁 Project Structure

```
ai-tutor/
│
├── backend/                         # Python Flask backend
│   ├── app.py                       # Flask app entry point
│   ├── config.py                    # Config (reads .env)
│   ├── requirements.txt             # Python dependencies
│   ├── .env.example                 # Copy to .env and add your key
│   ├── index/                       # Generated FAISS index + chunk metadata
│   │
│   ├── routes/
│   │   └── chat.py                  # /api/chat and /api/simplify endpoints
│   │
│   ├── services/
│   │   ├── rag.py                   # FAISS index build + search + exercise lookup
│   │   ├── embedding.py             # Sentence-transformers embeddings
│   │   ├── ai_client.py             # Gemini API wrapper (retry/backoff, safety settings)
│   │   └── prompt_engine.py         # Subject/mode/language-aware prompt builder
│   │
│   └── utils/
│       ├── chunker.py               # Paragraph + maths-exercise-aware chunking
│       └── text_cleaner.py          # Raw text cleaning
│
├── frontend/                        # Vanilla HTML/CSS/JS frontend
│   ├── index.html
│   ├── style.css
│   └── script.js                    # Chat UI, per-chat history, cross-chat memory (localStorage)
│
├── scripts/
│   ├── prepare_data.py              # Clean + chunk textbook .txt files → data/chunks.json
│   └── build_index.py               # Build FAISS index from chunks.json
│
├── data/                            # Raw textbook text files, one per class + subject
│   ├── eightmaths.txt / eightscience.txt / eightsst.txt
│   ├── ninemath.txt   / ninescience.txt   / ninesst.txt
│   ├── tenmath.txt    / tenscience.txt    / tensst.txt
│   └── chunks.json                  # Generated by prepare_data.py
│
└── README.md
```

---

## ⚙️ Setup Instructions (Step by Step)

### Step 1: Clone / Download the project

```bash
cd ai-tutor
```

### Step 2: Set up Python environment

```bash
# Create a virtual environment (recommended)
python -m venv venv

# Activate it
# On Windows:
venv\Scripts\activate
# On Mac/Linux:
source venv/bin/activate
```

### Step 3: Install dependencies

```bash
pip install -r backend/requirements.txt
```

> ⚠️ First install downloads the `all-MiniLM-L6-v2` embedding model (~80MB). This only happens once.

### Step 4: Add your Gemini API key

```bash
# Copy the example file
cp backend/.env.example backend/.env

# Open backend/.env and replace with your actual key:
# GEMINI_API_KEY=AIza...your_key_here
```

Get a free Gemini API key at: https://aistudio.google.com/app/apikey

### Step 5: Add textbook content

Each class + subject has its own file in `data/`:
- `eightmaths.txt`, `eightscience.txt`, `eightsst.txt` (Class 8)
- `ninemath.txt`, `ninescience.txt`, `ninesst.txt` (Class 9)
- `tenmath.txt`, `tenscience.txt`, `tensst.txt` (Class 10)

Missing files are auto-created as empty placeholders the first time you run `prepare_data.py`.

### Step 6: Process the textbook data

```bash
python scripts/prepare_data.py
```

This cleans the text and splits it into chunks (maths gets exercise/question-aware chunking so "Exercise 5.8, Q3" can be looked up directly), saving `data/chunks.json`.

### Step 7: Build the FAISS index

```bash
python scripts/build_index.py
```

This creates the vector embeddings and saves the FAISS index + chunk metadata to `backend/index/`.

### Step 8: Start the Flask backend

```bash
python backend/app.py
```

You should see: `🚀 AI Tutor backend started at http://127.0.0.1:5000`

### Step 9: Open the frontend

Open `frontend/index.html` in your browser (double-click or use Live Server in VS Code).

---

## 🔁 How RAG Works

```
Student Question
      ↓
  Maths "Exercise X.Y, Q N"? → direct metadata lookup (skips semantic search)
      ↓ (otherwise)
  Embed question (sentence-transformers) → search FAISS
      ↓
  Top-k relevant chunks, filtered by subject + class ("standard_8/9/10")
      ↓
  Build prompt (subject rules + answer depth + language + conversation
  history + cross-chat memory + retrieved context + question)
      ↓
  Send to Gemini API (with retry/backoff on rate limits)
      ↓
  Return answer + chunks used to the student
```

---

## 💡 Features

| Feature | Description |
|---------|-------------|
| 📚 Subject-aware answers | Different prompt strategies for Maths, Science, Social Science |
| 🎓 Class-aware retrieval | Separate textbook content and search index per class (8, 9, 10) |
| 💬 Simple / Detailed mode | Student can choose explanation depth |
| 🌐 English / Tamil answers | Full response translation with bilingual technical terms |
| ✨ Explain Simpler | One-click to re-explain the last answer in even simpler language |
| 🔢 Exercise lookup | Recognises "Exercise 5.8, Q3" style questions and answers the exact textbook problem |
| 🧠 Conversation memory | Multi-turn chat history feeds back into the prompt for natural follow-ups |
| 🗂 Cross-chat memory | The tutor can recall relevant snippets from the student's other saved chats |
| 🕘 Chat history & persistence | Multiple conversations saved locally (localStorage), switchable from the sidebar |
| 🌗 Light / Dark theme | Theme toggle with no flash-of-wrong-theme on load |
| 📱 Responsive UI | Collapsible sidebar, works on mobile and desktop |
| 🔍 RAG | Answers grounded in actual Samacheer Kalvi textbook content |

---

## 🛠 Tech Stack

| Layer | Technology |
|-------|-----------|
| Backend | Python 3.10+, Flask 3, flask-cors |
| Embeddings | sentence-transformers (all-MiniLM-L6-v2) — **FREE, local** |
| Vector DB | FAISS (faiss-cpu, flat L2 index) |
| LLM | Google Gemini (`gemini-2.5-flash-lite`) |
| Frontend | Vanilla HTML5, CSS3, JavaScript (no framework, no build step) |

---

## 📝 API Reference

### GET /health
Simple liveness check → `{ "status": "ok", "message": "AI Tutor backend is running!" }`

### POST /api/chat
```json
// Request
{
  "question": "What is photosynthesis?",
  "subject": "science",           // maths | science | social
  "mode": "easy",                 // easy | detailed
  "language": "english",          // english | tamil
  "class": "9",                   // 8 | 9 | 10 (defaults to 9)
  "history": [{ "role": "user", "text": "..." }, { "role": "ai", "text": "..." }],
  "crossChatMemory": [{ "role": "user", "text": "...", "chatTitle": "..." }]
}

// Response
{
  "answer": "Photosynthesis is the process by which...",
  "chunks_used": ["Photosynthesis is the process...", "..."]
}
```

### POST /api/simplify
```json
// Request
{
  "text": "Previous AI answer to simplify...",
  "subject": "science",
  "language": "english"
}

// Response
{
  "answer": "Simpler version of the answer..."
}
```

---

## 🔧 Troubleshooting

**"FAISS index not found"** → Run `python scripts/build_index.py` first (after `prepare_data.py`).

**"GEMINI_API_KEY is not set"** → Create `backend/.env` from `.env.example` and add your key.

**"Could not connect to backend"** → Make sure `python backend/app.py` is running on port 5000.

**Slow first response** → The embedding model loads on first use. Subsequent responses are faster.

**Gemini rate-limited (429)** → The backend automatically retries with backoff; if the daily free-tier quota is exhausted, requests will keep failing until it resets.

---

## 📖 Adding More Textbook Content

Append more text to the relevant `data/*.txt` file for that class and subject, then re-run:

```bash
python scripts/prepare_data.py
python scripts/build_index.py
```

The backend will automatically use the updated index on next restart.
